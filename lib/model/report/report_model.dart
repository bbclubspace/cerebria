import 'dart:ui';

class Report {
  String title;
  DateTime startTime;
  DateTime endTime;
  Color color;
  bool isAllDay;

  

  Report(this.title, this.startTime, this.endTime, this.color, this.isAllDay);
}
